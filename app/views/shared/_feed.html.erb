<% if @feed_items.any? %>
<ol class="microposts">
    <%= render @feed_items %>
</ol>
<%= will_paginate @feed_items ,params: { controller: :static_pages, action: :home } ,:previous_label=> '上一页',:next_label=> '下一页'%>
<% end %>